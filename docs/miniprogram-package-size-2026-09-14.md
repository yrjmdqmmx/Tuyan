# 小程序 3.5.4 包体积优化与验收

2026-09-14；源码 `codex/miniprogram-mobile-ui-20260914@e44212455718ff20921e580ba453bd490a142217`。实际开发工具项目为 `/Users/a1-6/WeChatProjects/miniprogram-9`。

## 问题与改动

原生代码质量检查有两项未通过：主包超过 1.5M、图片和音频资源合计超过 200K。此前上传已由用户完成；上传成功与体积建议通过、审核、发布是分别核对的状态。

- 原目录将同一组能力、比例、来源等元数据重复写入各型号，生成后的 TS 与 JS 又各自包含整份目录。构建脚本现在对重复 JSON 值去重，生成一个共享本地 JS 数据文件，客户端恢复与 Web 完全相同的对象。保留全部 739 个内置型号、顺序、角色能力、区域、申请指南、离线兜底及动态目录逻辑；没有改变目录版本或网络契约。
- 还原时不缓存可变对象，每个型号仍持有独立数组和对象，防止一个型号的处理影响另一个。
- 将小程序中 92,260 字节的观猹 SVG 替换为从同一原稿导出的 512×512 PNG，22,308 字节，保留透明圆角。网页端原稿 `apps/web/public/brand/watcha-rounded.svg` 与被替换的 SVG 字节相同，继续保留。导出使用 Sharp 的 `resize(512, 512).png({compressionLevel: 9})`，没有重画图标；可使用仓库 API 项目已声明的 Sharp 依赖重建。
- Logo 与联系二维码原文件及 SHA256 均保持，未增加远程图片依赖。
- 保持 TypeScript 编译插件、JS/WXML/WXSS 压缩和未用文件过滤配置；没有通过关闭扫描或排除需要的模型/资源让检查通过。

## 体积与验证

| 项目 | 修改前 | 修改后 |
| --- | ---: | ---: |
| 本地目录，包含 TS 和 JS | 3,655,797 B（工具显示 3.49MB） | 1,556,074 B（工具显示 1.48MB） |
| 未压缩运行文件，不含 TS | 2,110,850 B | 1,136,695 B |
| 图片合计 | 259,930 B | 189,978 B |
| 主包 1.5M 检查 | 未通过 | 已通过 |
| 图片/音频合计 200K 检查 | 未通过 | 已通过 |

上述本地文件统计不冒充最终上传压缩包大小。

- 代码质量原生重新扫描：2026-09-14 21:26，提示“小程序表现良好，未发现代码质量问题”。13 项全部通过，已在对话中提供主包、图片项通过的真实开发工具截图。
- 源码与上传副本各 117/117 测试通过；源码及副本类型检查通过，源码构建通过；目录生成器 `--check` 无漂移，Web/后端生成结果没有变化。
- 新增完整还原 SHA256、与 Web 未压缩目录逐字段相等、嵌套对象无共享引用，以及 1.5MiB/200KiB 体积预算检查。Node 本机冷加载目录约 8ms，仅作本地测量，不能代表真机性能。
- 开发者工具 2.02.2608070 / 基础库 3.16.0：Google 渠道 → Google 厂商 → 11 个主模型正常，Gemini 3.8 Flash 仍为已选；取消浏览后，三个角色仍为原有 Google Gemini / 观猹 Seedream / 观猹 Qwen 配置。
- 用户现有方法 158 字、图注 28 字、负向词 44 字保持；账户消费授权仍已连接；新观猹图标实际显示正常，教程显示 `miniprogram-3.5.4`，最终返回原精修页。

## 同步和边界

215 个受控文件与源码 checksum 一致：更新 11、新增 5、移除被替代的 SVG 1；修改前受控文件已备份，9 个本机私有/辅助文件哈希不变。详情见 [同步清单](evidence/miniprogram-3.5.4/local-sync.json) 和 [结构化验收](evidence/miniprogram-3.5.4/validation.json)。

身份管理页面的 `/api/auth/watcha/mini-status` 仍返回 404，页面明确提示网关尚未部署小程序身份接续接口；它与既有 TokenDance 消费授权独立。原生访问时记录了该错误，没有隐藏日志或修改后端。此项不属于本轮包体积修复，也不能由代码质量扫描通过推定为已解决。

本轮未发起模型消费、授权或支付，未上传新版、提交审核或发布。用户此前上传的开发版本不会被本地同步自动替换；后续如上传本轮代码，版本号应使用 `3.5.4`。
