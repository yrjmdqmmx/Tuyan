# 微信小程序 3.1.0 契约补齐与本地交付

## 基线与边界

- 权威仓库：`https://github.com/yrjmdqmmx/Tuyan`（GitHub API 已确认，旧 `Tuyan-clients` 重定向至此）。
- 2026-09-09 拉取主线：`27aa954d168e7e0ed05d6b2251965161f8d66e30`。
- 实施分支：`codex/miniprogram-parity-20260909`，同名隔离 worktree。
- 保留历史：`codex/miniprogram-account-security-3.0.1@8fe6b8d` 的 16 个提交；共同祖先 `eb31b2b`。主线在此之后有 308 个提交，不能以任一目录整体覆盖另一目录。
- 上传副本：`/Users/a1-6/WeChatProjects/miniprogram-9`，非 Git。私有配置、登录态及额外本机文件保留。
- 本轮只做源码、本地原生验收和受控同步；不上传、不提审、不发布、不改变生产后端、不执行付费供应商请求。

## 初始缺口清单（先审计，后实施）

| 优先级 / 功能 | Web / 后端当前行为与依据 | 小程序与上传副本现状 | 复用 / 适配及依赖 |
|---|---|---|---|
| P0 账号安全保留 | #168 生命周期 v3；#174 状态观察；#176 浏览器手动确认 | 主线仍缺 3.0.1 的完整找回/改密/重发 UI；上传副本有这些功能但缺新生命周期 | 合并旧 16 个提交，保留 `account/status`、202 accepted 和 review_required；`components/account-settings`、`utils/session` |
| P1 邮箱验证状态 | #174 `verificationStatusToken` + 只读 `POST /api/auth/verification-status`；#176 验证不自动登录 | 旧 3.0.1 等待页不感知用户已在邮件浏览器中确认；主线兼容勾选不代表实现 | 仅内存观察凭证，前台刷新、隐藏停止、过期/失败可恢复；无 token 兼容旧服务；不实现邮箱枚举或在小程序消费邮件验证 token |
| P0 目录及请求合法性 | #166/#170/#172/#185/#186，21 渠道、677 静态项、动态 OpenRouter、区域/尺寸/比例/编辑专用能力 | 主线已有生成器同步的目录、排序、区域、45 候选比例、分页视图；上传副本仍旧版本 | 直接复用最新源码并原生验证；补有效角色/格式/尺寸/比例/编辑类型和区域校验，防旧选择构造非法请求；`model-registry`、`model-routing`、`payload`、`refine` |
| P1 生成输入优化 | #152 `optimizeInputs` 与 `inputOptimizationContractVersion`，方法/图注/负向提示词；#178 区域透传 | 小程序无入口；`normalizeModelRegistry` 丢弃能力声明；SYNC 仅写向后兼容 | 适配三个字段的原文/候选预览、明确采用、取消/重试、单步恢复；只用主模型单 Key，失败不改原文，无自动重试 |
| P1 上传原图精修 | #178/#179 `refineUpload`、`finalizeReferenceUpload(purpose=refine)`、`sourceImageUpload`；生命周期归属与独立快照 | 小程序只可选前 50 个任务结果；无上传、替换、清理；registry 丢弃能力声明 | 按声明验证 MIME/字节/像素与模型限制；选择相册/文件，实际上传和校验阶段、失败重试、替换/移除清理；沿用生产后端 |
| P1 精修指令优化与结果 | #178 `inputOptimizationTargets` 包含 editInstruction；独立任务轮询与结果操作 | 小程序无优化，隐藏后轮询不恢复，结果无保存操作 | 精修上下文独立，只提交 editInstruction；补前后台轮询和详情/预览/保存，微信文件分享与真机边界分开记录 |
| P0 账号数据隔离 / 任务记录 | #168 不可变 ID 与 lifecycle；Web 任务源随用户清空 | records 已有部分 ID 检查；refine.loadSources 缺少换账号后的响应隔离；本机记录未按所有者隔离 | 补换号/退出时在途结果作废，来源与结果清理；任务卡和详情补精修指令、模式、来源及区域，不保存 Key/凭证 |
| P1 错误处理 | #157 健康请求有界重试；#168/#178 等新增稳定错误码 | 主线已有健康专用重试与连接错误中文提示，上传副本没有；BusinessError 提前返回绕过生命周期文本映射 | 复用只读重试，补生命周期/优化/上传/型号/地区错误映射；生成与精修保持单次请求 |
| P2 参考图库 | `referenceLibrary` 原检索/分页/精确 ID 契约保留；#74/#85 为公开评测增加证据与题集 | 小程序已有 12 条分页、双筛选、搜索、跨页 10 项、精选模板；没有证据显示工作台查询契约改变 | 直接复验真实公开查询、筛选重置与选择边界；不把目录兼容写成原生已验收 |
| 无需适配 / Web 专用 | #181 站长后台、#74/#85 评测证据发布与管理、#158 匿名 MCP、OpenAcad 外链 | 属独立后台、公开站点或 Agent 工具，不改变生成/精修/任务客户端协议 | 保留 Web 入口和兼容性，不在小程序内复制管理/发布操作。手动邮箱确认继续在第一方邮件网页完成 |

### 未被同步勾选覆盖的事实

1. 最新主线仍标记小程序版本 1.0.0，且没有未合并的 3.0.1 账号安全测试；盲目同步会退回账号功能。
2. #152/#174/#178 的“兼容/新增可选字段”只证明旧请求继续可用，输入优化、验证观察、精修上传尚未在小程序实现。
3. 小程序主线已存在但上传副本没有的 8 次相关提交：`7e9cdb8`、`8a69ba7`、`8d9c1b7`、`8cbd415`、`18e2a89`、`b4bbc0f`、`b96d507`、`e911b4b`。
4. 上传副本此前有 3 个旧测试和 2 个缺失测试；应同步完整受控测试集，不能用副本的通过结果代替权威源验收。

## 验证及交付记录

- 主线初始基线：22 个测试文件通过，0 失败。账号 3.0.1 的测试将在合并后恢复并与新契约一同运行。
- 后续记录将在实施和实际验收后填写；本文件的初始清单不代表完成。
