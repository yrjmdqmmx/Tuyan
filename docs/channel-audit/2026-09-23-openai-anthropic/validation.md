# 本地验收记录

日期：2026-09-23。基线 `origin/main@54c90ad72b14105400704ecfbc35a3b9ca7a584b`，分支 `codex/openai-anthropic-refresh-20260923`。工作目录为隔离的 `model-catalog-channels-20260921` 工作树；Desktop 旧 checkout 保留。目录版本 `2026-09-23.v25`。未提交上线、未 push、未部署，本文不构成生产验收。

## 自动化验证

| 检查 | 结果 |
|---|---|
| 根目录契约测试 | 20 / 20 |
| Core 全量测试 | 1,015 / 1,015 |
| Web 全量测试 | 519 / 519 |
| 小程序全量测试 | 121 / 121 |
| Core TypeScript 与打包 | 通过 |
| Web Vite 构建 | 通过；已有大包提示仍存在 |
| 小程序 TS 编译 | 通过；不是微信平台上传或真机验证 |
| 目录、思考配置、版本报告生成器 `--check` | 无漂移；1,032 个静态型号 / 1,155 个思考 profile |
| `git diff --check` | 通过 |

新专项覆盖：

- 3 个原厂精确 ID + 5 个 OpenRouter 同步 ID，分别走真实构造器的主模型与参考图识别路径；逐档验证 Responses / Messages / OpenRouter Chat 字段、协议、实际 URL 和图像输入。
- 未提供配置与空配置都不发送思考字段；OpenAI mode 与 effort 独立；Claude 原厂不发送不支持的 thinking 开关或预算；OpenRouter 不透传原厂 mode。
- 不支持的图片输出角色、虚构原厂 `-pro`、`ultra`、非法预算、跨型号/协议配置在发送前拦截。公共目录中 5 个 `:batch` 由现有规则排除，不作为同步 API 上架。
- 保留 usage、请求 ID、实际型号和公开价；公开报价、渠道响应 cost、账单结算分别记录。拒绝、空回答、截断保留用量，不触发第二次请求或自动回退。
- 老客户端未携带 thinkingConfig 时，新身份仍保存加密检查点；重启后复用已完成步骤。原任务思考快照固定；结果未知、并发恢复、跨账号请求不能触发重复推理。旧 TokenDance 登录/恢复错误语义保持。
- 历史目录源逐项比较：既有条目的身份、能力、默认值、禁用状态、生命周期、地区及别名均无改动；新增项单独验证，历史契约指纹未替换。
- 新直连型号沿用现有上传处理：最多 8 张；OpenAI 单图 16 MB / 合计 48 MB / 整包 50 MB，Anthropic 单图原始 7 MB / 合计 21 MB / 整包 32 MB。不是放宽到厂商通用最大额度。

## 桌面与移动端

Chromium 本地验收，1440×1000、390×900、320×900。静态目录取本次生成文件，OpenRouter 目录由后端实际解析本次只读公开目录快照得到；运行环境显式启用既有 thinking v1。身份、健康、目录与参考库读取均被本地 mock 拦截，未传入真实 Key；推理、创建任务、恢复任务和外网请求均未发生。

- 中文名称和准确 API ID 均可搜索；Sol / Luna / Opus 5.5 可独立选择。
- 主模型 Sol 显示六种 effort 与 standard/pro；视觉 Opus 5.5 仅五种 effort；不支持的图片模型不出现思考控件。
- Sol → Luna 清空不兼容参数，返回 Sol 恢复对应偏好；主模型和视觉设置互不覆盖。刷新后重选相同身份恢复其偏好；未改变既有页面模式初始化行为。
- OpenRouter Sol Pro 只有渠道确认的 effort；OpenRouter Opus 帮助显示其渠道默认 high，原厂默认 medium 独立保存；界面“默认”依旧为空选项。
- 390 / 320px 控件不越界、页面无横向溢出；移动端渠道 → 厂商 → 型号选择、搜索与切换均通过。
- 页面错误 0，非允许请求 0。未执行真实浏览器账号登录、移动设备或微信验收。

本机可复查证据目录：

- 测试日志：本机验收归档 `tuyan-openai-anthropic-20260923/`。
- 浏览器脚本、截图和断言：本机截图归档 `tuyan-model-refresh/`。
- `desktop-settings.png`：原厂分角色配置。
- `desktop-openrouter.png`：聚合路由独立配置。
- `mobile-390-settings.png`、`mobile-320-settings.png`：移动布局。
- `mobile-model-picker.png`：移动搜索及准确 ID。
- `checks.json`：布局坐标、操作及错误记录；`qa.mjs`：可复现的网络隔离脚本。截图不包含凭据。

## 剩余缺口

真实 Key 的单模型权限、地域准入、额度与速率、真实图像理解质量、延迟和账单均未验证。Sol/Luna 专属图片 detail/patch 细则证据仍不足，保留已有保守上传策略。12 个其他渠道未取得这些候选模型的充分上架证据，不新增猜测 ID，也不据此认定退役。小程序原生思考编辑 UI 及真机验证仍为既有欠项；共享目录和原快照透传已经同步。
