# Tuyan 4.0.0 开发集成记录

2026-09-23，负责人明确论文画布归属后续 **4.0.0**，不纳入 **3.8.0**。本文记录开发集成，不是发布证明。

## 基线与分支

- 3.8.0 最新已合入基线：`0b60f85c8387ddc41b05b55c3952c36f685f42fa`（main，PR #232）。
- 4.0.0 开发分支：`codex/tuyan-v4.0.0`，保留画布 `99c194be98058a1976853d386baee65532a9d09e` 全部历史，再 merge 3.8.0 基线。
- 原分支 `codex/figure-studio-20260921` / 草稿 PR #225 是前序研发记录；独立工作树继续使用，Desktop 主检出和其他任务检出不动。
- PR #226 / `f547344`：v23 精修、TokenHub/MiMo/Runware 目录及调用适配；#227 / `d651519`：部署记录。
- #228 / `d40f1a8`：官方五渠道、三角色思考/恢复、全局语言、分组导航和更新日志；#229 / `54c90ad`：3.8.0 发布记录。
- #230 / `9a52aa7`：v25 GPT-6 Sol/Luna 与 Claude Opus 5.5，1,032 静态身份及 OpenRouter 同步路由；#231 / `e304b7d`：追加发布记录。
- #232 / `0b60f85`：公开排行榜开关独立、Benchmark v2.5 帕累托/双语归属。
- 相邻任务 `codex/miniprogram-v380-parity-20260923` 的未合入工作没有纳入；不读取或覆盖其未提交修改。

## 合并处理

保留 3.8 分组导航，将论文画布添加到唯一共享入口表，紧邻工作台；桌面和手机使用相同配置。登录外壳保留画布的账号代际保护和统一弹窗，并兼容 3.8 更新日志/排行榜；保留 3.8 双语和模型/思考设置，不还原旧导航或旧目录。

工作台生成、参考图、方法/图注输入、精修及账户流程保持；画布继续独立 `/figure-studio/`。可滚动的编辑区高度、面板折叠、展开、缩放和平移保留；不改变 FigureSpec 毫米尺寸、字体或导出实现。

4.0.0 只写入维护草稿。已发布 3.8.0 的公共日志原样保留，不创建虚假发布日期或改变包内部版本号来冒充正式发布。

## 验收

- Web 全量 625/625；Core API 全量 1,055/1,055；API TypeScript 检查和 Web production build 通过。另新增 4.0 草稿不进入已发布 3.8 断言，单独复验。
- 导航及关联页面/会话针对测试 88/88；独立审计通过 v25 1,032 静态目录、1,155 thinking profiles、参考上传契约同步检查，以及共享 14、Gateway 46、HTTP 23 项检查。
- 合并暴露并修复实际 P1：画布全局 workflow wrapper 漏转发新增 hooks 与角色，会把工作台 `thinkingContractVersion` 降到 0 并影响检查点。新增三个真实 legacy 组合回归，修复前 3 项均失败；修复后角色参数、加密恢复及画布隔离均通过。
- Chrome 实际使用正常登录会话，两页均识别同账号，TokenDance 保持已连接。未登出用户或触发推理。
- 1440×900、DPR=1、visualViewport.scale=1：两页工作台/画布/排行榜/openacad/更新日志的 x 分别为 381/466/563/648/757.45px，前两项宽度 80/92px、间距 5px；统一顺序、字体、当前页标记，页面均无水平溢出。工作台测试专用消费提示条使整体 header y 不同，该提示不属于公共导航，也不会发布。
- 工作台：实际切换生成/精修后临时输入保留，验后还原；三角色设置、OpenAI 搜索 `gpt-6-sol`、主模型选择及思考强度/标准-Pro 控件正确，识图/图片角色保持，验后恢复 Qwen3.8 Flash 原选择。未提交任何模型调用。
- 画布：编辑区 738px、整页 1019px 可自然滚动。左右折叠/恢复、展开/还原、适配、放大、抓手平移均实际操作；放大后 scrollLeft 从 172px 到 312px。源稿保持 183×70mm、18 对象、版本 5。
- 390×844 与 320×740：手机“更多”两入口同一行，论文画布紧邻工作台右侧；从工作台点击可到独立页。中英文菜单无横向溢出。手机沿用查看/导出/规则模式，未宣称新增触屏对象编辑。
- 截图归档：`~/.codex/visualizations/2026/09/23/tuyan-v400-baseline-navigation/`，含 desktop 两页、画布全页/展开、工作台思考设置、390/320px 菜单。构建原有 bundle 大小提示仍存在。

## 未完成门槛

未合并 main、未部署 4.0.0。上一轮真实观猹规划/编辑各一次和格式相关 Inkscape 验收仅为历史证据；本轮不追加付费调用，不把 UI 回归当作完整真实联调。通用 API 真实连接仍由用户暂缺。更多导出兼容边界见 2026-09-22 集成验收与发布清单。
