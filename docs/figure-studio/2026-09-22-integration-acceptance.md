# 论文画布公共接入与验收 · 2026-09-22

## 当前状态

本轮已实现公共模型接入、持久操作记录与恢复；本地回归和部分真实浏览器/文件验收通过。**尚未完成真实模型联调或上线。** 不把合成模型测试、文件可打开、文字可提取等同于真实推理或外部编辑完整通过。

- 已同步 main `0d8b0b4500eb9ca6d8b1b5180450276c7f78b2fb`（#224）。论文画布已有 `0374995`、`9ebba0f`、`632af60`、`29b3101` 仍属于独立 `codex/figure-studio-20260921`。
- 已通知并核对“恢复 Tuyan 模型目录交接”的精修分支；未导入其未提交工作，也未更改其 `provider-workflow.ts`、`provider-egress.ts`、`jobs.js` 或运行中的 5173/8791 服务。
- 本轮共享改动限于 figure API/类型、通用 API 面板可选角色、TokenDance 文本调用可选输出上限及对应生成代码。工作台默认角色仍为 main/vision/image，保存画布设置会保留另外两个角色。

## 公共能力及故障边界

账号和会话沿用网站 AuthProvider/Gateway；使用原模型目录、ModelPicker、GenerationSettingsDrawer、NativeCredentialFields、TokenDancePanel 与通用 API 连接信封。仅画布材料、结构方案、对象命令、文档版本校验属于专有逻辑。

Plan/Edit 使用客户端固定 requestId + 完整图稿 SHA-256。重复 ID 的相同请求查询原结果，不同输入拒绝；网络错误只查询原 ID。账号代际与租约同时在传输前核验，同账号跨实例最多 2 个在途操作。结果/检查点加密暂存 7 天，账号注销清理；本地仅存无密钥的操作指针。未知付费状态不重放，只有共享渠道恢复机制判定安全时才能显式恢复。原生渠道失败暂不开放恢复。

独立审查复现并修复了账号切换时目录加载竞态、跨实例限额竞态、重启恢复状态不匹配、旧租约恢复后越过新槽位的四个问题；回归使用合成传输，不是实际供应商验收。

## 验证证据

| 层次 | 结果 | 限制 |
| --- | --- | --- |
| Core | 全量 683/683；随后新增最后租约用例，operation 18/18；本轮最终导出提示修改后 figure service 16/16；类型与 build 通过 | 无真实供应商调用 |
| Web | 完整 561/561，目标 86/86，build 通过 | 保留现有大 chunk 提示 |
| Gateway | 定向 app 46/46 通过 | 真实账号本地登录尚待用户完成 |
| 共享 SDK / 本地 auth / runtime gate | 17/17 通过；模型目录 760 项生成无漂移 | 不代表推理成功 |
| Linux 导出 | 真实 linux/amd64 非 root、只读、断网、1.5 CPU/3 GB、128 MB tmpfs 转换；sharp 回归通过 | 运行时约新增 386.1 MiB 系统包；本地临时 Debian 镜像源覆盖，仓库保留官方源 |
| 浏览器 | Chrome 桌面 1440×900、移动 390×844；同尺度导航、面板折叠/还原、适合窗口与抓手实际操作；网页文字编辑、撤销、源稿保存重开、SVG 下载完成 | 当前真实本地账号未登录，工作台显示既有登录 gate；不冒充已验证登录态生成 |

抓手实测中 SVG 屏幕位置由 (-139, 192.984) 移至 (1, 221.984)，`width=183mm`、`height=70mm`、对象 14 个、版本 0 不变。文字修改只改所选标签，版本增至 1，撤销恢复文字且版本增至 2。移动端沿用既有阶段范围：查看/导出/规则检查，对象编辑仍需宽屏电脑。

浏览器实际下载 SVG 4056 字节，SHA-256 `079a400815d112b57b4580602ffff31039dbd1300effda8ec4054550f51d5103`，与页面报告一致；下载的源稿重开后仍是 183×70 mm、14 个对象。此为明确标注的结构示例，不是模型生成结果。

## Inkscape 真实兼容性

Linux Inkscape 1.2.2 导出，macOS Inkscape 1.4.4 手动导入/编辑。原始文件与截图保存在本机证据目录 `/Users/a1-6/.codex/visualizations/2026/09/22/tuyan-figure-integration/`。

| 格式 | 打开/外观 | 文字 | 独立对象 | 保存后重开 |
| --- | --- | --- | --- | --- |
| SVG | 可打开；字体按本机回退 | 单独将 Input 123 改为 Input 123 edited，其他 5 标签未变 | 6 个独立 text 保留；矩形可单独移动 | 另存 SVG、关闭、重开通过；89×70 mm 不变 |
| PDF | 实际文件解析渲染通过；初始导入显示所有标签 | 中文及 α β ≥ 10 μm 精确提取通过，但 Inkscape 内部导入合并全部 6 个标签 | 失败：合并文字编辑后排到白底/彩色框之前，被遮挡；矩形仍可单独操作 | 编辑兼容已失败；未将 SVG 中间副本当作 PDF 同格式回环通过 |
| EPS | 实际解析与渲染通过 | 中文/Latin 可提取；科学符号编码失败 | 未通过完整独立对象编辑验收 | 未验证 EPS 同格式回环 |

PDF 问题不是仅去掉白底能解决；彩色框仍会遮挡合并文字。转曲会丢失文字编辑能力，也不能作为满足要求的替代。页面及服务能力说明均明确 PDF/EPS 兼容性受限；继续编辑推荐源稿/SVG，最终文件核验仍保留人工确认。期刊自动格式检查不代表科学内容或 AI 政策已通过。

## 真实联调与发布门槛

用户已授权本轮最多 6 次文本调用、累计最高 ¥1，不充值、不自动重试、不调用图像模型。当前 **实际付费调用 0 次，实际调用支出 ¥0**。

- 真实本地预览 `http://127.0.0.1:5290/figure-studio/` 已连正式账号服务；本地 Mongo、源稿与加密凭据独立，不接生产 DB。用户需要在共用登录窗输入现有账号凭据；不提取正式浏览器 Cookie 或伪造身份。
- 正式站点已观察到账号登录与 TokenDance 连接；这不是本地授权凭据或本轮推理成功证据。
- TokenDance Qwen3.8 Flash 官方目录当日可见输入 ¥0.8、输出 ¥2.7/百万 token，画布上限 4096 输出 token。先做规划+修改两次，小型合成材料，不自动追加失败调用。
- 用户暂不能提供通用 API 连接和 Key。该真实链路明确未验证；本地消费脚本在 DNS/传输前拒绝 custom 推理并标记 not_sent。
- 真实链路、兼容性和远程 CI 门槛未关闭前保持 draft，不合并、不触发部署。后续发布按 [清单](2026-09-22-release-checklist.md) 使用固定 SHA/digest 和现有 GitHub 环境审核，不绕过 required reviewer。
