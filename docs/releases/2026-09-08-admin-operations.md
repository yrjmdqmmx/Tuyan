# 站长运营后台发布记录

实现 PR：[#181](https://github.com/yrjmdqmmx/Tuyan/pull/181)。Web、Auth Gateway、Node Core 与 Benchmark companion 实际发布 SHA：`fc962212bfa497aab91c770a4edfa7050c027e93`。本记录为发布后的文档提交，不代表再次部署。

站长入口现统一展示真实总览、账号与用户、近期任务、社区评估题和反馈。支持服务端搜索、筛选、排序、分页，用户与任务/社区记录互跳，返回保留列表上下文。任务跟进与执行状态分开；社区编辑和审核保留原文、处理记录与版本冲突保护。原高级评测能力保留独立入口。

发布前已合入主线精修上传及恢复身份路径修正（PR #178/#179）。先通过完整 CI 和镜像来源核验，再升级香港服务、确认索引和健康，最后发布 Web。无新增环境变量或权限扩大；三份业务镜像固定摘要并对应相同代码 SHA，Plot/Mongo 镜像保持原摘要，评测执行继续关闭。

## 发布门禁

| 项目 | 结果 |
| --- | --- |
| 分支 / PR CI | [34200985740](https://github.com/yrjmdqmmx/Tuyan/actions/runs/34200985740) / [34201028142](https://github.com/yrjmdqmmx/Tuyan/actions/runs/34201028142) 通过 |
| 合并 CI | [34201407241](https://github.com/yrjmdqmmx/Tuyan/actions/runs/34201407241) 通过 |
| 香港部署 | [34201770334](https://github.com/yrjmdqmmx/Tuyan/actions/runs/34201770334) 成功，沿既有 production 环境审批和主机锁 |
| Web Pages | [34202384769](https://github.com/yrjmdqmmx/Tuyan/actions/runs/34202384769) 成功，排行榜保持开启 |
| 服务与配置 | 5 个服务健康；Core/companion 内置 provenance 与镜像 revision 匹配；维护解除，除 code SHA 外容器环境指纹一致，SG 出口保留 |
| 索引与部署 smoke | 新索引全部存在；使用已有 createIndex 权限。事务、健康、隔离与未授权 Core 检查通过 |
| 公开接口与资源 | `/health`、`/ready` HTTP 200；8 个后台查询入口对匿名及伪造管理员请求头均返回 401；线上 HTML、主脚本、样式及后台 chunk 与 Pages 产物 SHA-256 一致 |

本地合入最新主线后：Gateway 140、Core 458、Web 361、共享 API 29 项测试通过；Core 类型检查及 Web/Core 构建通过；真实 Better Auth/Gateway/Core/Mongo 集成 66 项、既有账号生命周期集成和浏览器 15 条流程通过。完整范围与复现命令见 [实现与验收说明](../admin-operations-2026-09-08.md)。

## 生产浏览器验收

使用现有管理员登录态，只读检查真实数据。验收时总账号 16 个；近 7 天新增 2 个账号、1 条任务，切换近 30 天后为 6 个账号、17 条任务。全时段任务 91 条，失败筛选为 21 条，与数据库聚合结果一致。统计是读取时快照，后续会随业务变化。

- 用户列表默认邮箱脱敏；每页 10 条时第一页 10 条、第二页 6 条；进入详情并返回保留第二页和页大小，精确用户 ID 搜索返回 1 条。
- 任务失败筛选第一页 20 条、第二页 1 条；详情显示输入、失败原因和执行记录，返回保留状态与页码。成功任务 4 张结果图加载完成；从任务跳转到用户，再进入 45 条关联任务列表。
- 社区实际没有提交，页面显示真实空状态与完整审核状态选项；4 条反馈均通过真实接口加载，联系方式脱敏。未展开完整联系方式。
- 桌面 1440×1080、手机总览 390×843，以及 354px 任务列表均无文档横向溢出；任务宽表在表格容器内滚动。临时视口覆盖已恢复。
- 浏览器无应用脚本错误。总览、用户、任务、社区和反馈五类实际响应均为 HTTP 200、code 0、`Cache-Control: no-store`；抽查 JSON 未出现密码、令牌、密钥、IP 或原始 User-Agent 字段。

浏览器扩展的部分定位操作超时，改用 Chrome 原生控件完成分页、筛选、搜索及详情检查。后台数据和操作没有使用静态替身；这些工具通道超时不计作应用脚本错误。

## 边界与回滚

社区审核、编辑、任务跟进、版本冲突及权限拒绝在隔离的真实服务/数据库中测试，生产不修改真实账号、任务或社区状态。没有发送邮件、执行付费模型调用、启动评测或发布小程序。Laf 回退运行时不提供新增后台接口；尚无正式题集发布关联、任务取消/重试或用户封禁契约，页面如实保留这些限制。未进行大规模生产压测。

发布前后端 SHA `cca7061fa27c536431445ef3899344aeebe57f7d`，Web SHA `28000e0be7479cc6a5bc84e323443d86bbdc6bf1`。旧镜像锁及运行配置保存在主机受保护的 `admin-operations-20260908T074958Z` 备份目录（目录 0700、文件 0600），仓库仅记录安全引用和指纹。若需回滚，沿用既有香港部署审批与固定镜像流程，并将 Pages 发布至旧 Web SHA；不回退数据库或清理账号，新增索引可保留。本次未触发回滚。

结构化证据：[2026-09-08-admin-operations-evidence.json](2026-09-08-admin-operations-evidence.json)。
