# 科研精修与三渠道全目录 v23 发布

用户在本地验收后明确要求“上线”。发布代码为 `f547344d8019769eafd53d668bbcbd367210a609`，功能 PR [#226](https://github.com/yrjmdqmmx/Tuyan/pull/226)。之前文档中的“不部署”是实施阶段记录；本次仅发布 Web / Core 和配套出口配置，不充值、不触发付费推理，不上传或发布微信版本。

## 发布内容

精修辅助参考图、型号限定的遮罩与 BRIA 原生结构化编辑，以及 TokenHub / MiMo / Runware 的主模型、识图、生图和精修适配。官方目录核对 491 项，适配 183 个 SKU，完整取舍、价格和来源见 [逐型号审计](../channel-audit/2026-09-22/README.md)。确认下架或暂不可用的型号从可选列表隐藏；原有配置与历史身份保留，不自动换渠道或型号。

发布前补齐 Runware 的新加坡代理精确域名白名单；没有放宽来源、端口、私网或伪造域名限制。此前 Core 路由已指定海外出口，但代理未放行，真实只读 CONNECT 为 403；补齐后 CONNECT 200、无凭据 GET 返回上游 400。此证据仅证明网络路径，不能证明模型授权、推理或计费成功。

## 验证与固定镜像

- PR CI [35683441765](https://github.com/yrjmdqmmx/Tuyan/actions/runs/35683441765) / [35683438946](https://github.com/yrjmdqmmx/Tuyan/actions/runs/35683438946) 与主线 CI [35683825661](https://github.com/yrjmdqmmx/Tuyan/actions/runs/35683825661) 全部通过。
- 首轮完整 CI 揭示旧版目录数量/顺序快照断言及 Seedream 两项展示名大小写不一致；修正后重跑全部通过。没有跳过失败检查或放宽保留历史身份的约束。
- Core 860、Web 466、小程序 121 项测试通过；发布前另核对 Core 出口 11、SG 出口行为/契约 111、根目录跨端契约 20、HK 部署契约 228、Laf 安全契约 6 项。模型服务使用模拟传输，不是生产推理验收。
- Core [镜像构建](https://github.com/yrjmdqmmx/Tuyan/actions/runs/35683848669)：`sha256:81029ed394e7a19ce977361fa6ae8d00cde117ae3c098c8a83aca6a1f1bded94`。
- Benchmark companion [镜像构建](https://github.com/yrjmdqmmx/Tuyan/actions/runs/35683851463)：`sha256:faa8815f825e72045f6fb56955c5bbbdb1783c996e78c92caf8715d20120d801`。执行器仍为 `PAPERBANANA_BENCH_ENABLED=false`；保留既有公开排行榜，不运行新评测。
- HK [生产部署](https://github.com/yrjmdqmmx/Tuyan/actions/runs/35683970466) 通过正常环境审批并成功。运行镜像、OCI revision、环境代码 SHA、`build-provenance.json` 全部匹配发布 SHA；五个图研容器 healthy，维护模式解除，公开 `/ready` 正常。
- Core/Bench 环境仅代码 SHA 改变，排除此字段后的四份环境文件指纹前后一致；Gateway、Plot、Mongo 镜像保持不变。备份保存在主机私有目录，没有复制或公开密钥。
- 线上目录为 `2026-09-22.v23`、`refineControlsContractVersion=1`。TokenHub 41、MiMo 4、Runware 138 项的 ID、展示名、版本、API ID、角色、协议、研发方、可选状态及能力逐项核对一致；运行时上传约束另与共享 policy 核对，差异 0。
- SG 配置使用现有恢复身份和 HK 内网跳板更新，保留主机密钥验证。仅给 Squid 加入 `api.runware.ai` 并同步版本化安装脚本；原 WireGuard 配置指纹不变，原有无凭据供应商连通与明确拒绝边界检查通过。云助手读取命令因旧 Agent 离线而中止；安装接口只返回受理，未作为执行成功证据，也未用于修改代理配置。

## Web 验收

- [Pages 发布 35684170270](https://github.com/yrjmdqmmx/Tuyan/actions/runs/35684170270) 成功，使用同一固定发布 SHA。工作台、排行榜入口、主 JS/CSS、路由、精修及上传模块共 7 个线上文件 HTTP 200，字节长度和 SHA-256 与 Actions 发布产物完全相同。
- 生产 Chrome：MiMo 主模型 4 项、识图 3 项；TokenHub Kimi 系列和 Seedream、Runware 主模型与图像型号可选择。已下架的 `hy-image-v3.0` 搜索为零项；未来才到期的型号仍按实际生命周期显示。
- 生产精修页：原图和辅助图入口分离；Runware GPT-Image-2.5 Flare 显示辅助图上限 8、总图数 16（包含原图）及遮罩不能与辅助图并用的限制。原图为空时禁止提交，2K / 16:9 控件正常；没有上传或提交任务。上传、删除、切换不兼容、遮罩、BRIA 结构化及失败恢复的完整交互依据此前本地模拟验收，不能视作真实供应商结果。
- 已检查的桌面页面无横向溢出、控制台 error 为 0；没有重新验证真机或微信浏览器。验收临时切换的模型与模式已恢复为原 TokenDance 普通模式，Qwen3.8 Flash / Seedream 5.0 Pro / Qwen3.8 Flash，生成 1K / PNG / 16:9；原方法与图注保留。
- 可移植的非敏感记录见 [发布证据 JSON](2026-09-22-refine-v23.evidence.json)。本机截图与原始部署日志保存在 `/Users/a1-6/.codex/artifacts/tuyan-release-v23-20260922/`，不包含密钥；截图裁去账户栏。

## 边界

本次付费推理 0、充值 0。真实 Key 权限、各型号推理结果、科研质量、额度和账单仍未验证；不把目录/网络/模拟测试冒充真实模型验证。TokenHub 新加坡、MiMo 定制 UltraSpeed、专用多资产编辑工具等仍按审计表列为未接入。小程序共享源码已同步并测试，原生高级精修界面未移植，微信平台未上传、审核或发布。遗留 Laf 服务没有部署。
