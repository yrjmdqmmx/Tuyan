# 小程序观猹身份接续补发（2026-09-15）

## 已修复的问题

小程序已经包含观猹身份接续页面，但生产网关没有同步部署 `/api/auth/watcha/mini-status` 等接口，导致客户端显示 404 和邮箱登录降级。PR [#196](https://github.com/yrjmdqmmx/Tuyan/pull/196) 已合并，生产 Web/Gateway/Core/Benchmark 的源码版本为 `4077ddc60a0d7814a0c6a2ff0484fbae46c3413c`。现在接口返回 HTTP 200、`available: true`、`miniProgramSupported: true`。

网关继续复用已登记的 `/api/auth/oauth2/callback/watcha` 和原观猹配置，没有新增或轮换 Key，没有改变 TokenDance 消费授权。身份登录、绑定与模型消费授权仍独立管理。

## 生产发布与验证

- 主线 CI、Gateway/Core/Benchmark 固定摘要镜像、香港发布、Web Pages 均成功，版本和任务编号见 [证据 JSON](2026-09-15-miniprogram-watcha-evidence.json)。香港发布走正常 production 必需审核人批准，没有绕过审批或修改保护。Benchmark 执行仍关闭。
- 香港发布确认 Mongo 事务、健康/隔离/未授权 Core/Benchmark smoke 均通过，并清除维护模式。
- 9 项公开/匿名接口检查全部通过：健康、就绪、Web 和小程序身份能力、错误原生来源拒绝、原生发起到官方 S256 PKCE 跳转、启动票据重放拒绝、错误接续码拒绝、取消清理。
- 微信开发者工具账户身份页显示“已绑定观猹”，原 404 降级提示消失；TokenDance 继续显示已连接。
- 使用开发者工具实际 `wx.request` 的隔离 Cookie 会话发起登录，Chrome 在官方观猹页面完成既有身份登录并复制一次性接续码，再由 SDK 兑换。结果为 `complete`，账号 ID 与原账号一致，消费授权仍已连接。测试会话随后退出，原登录 Cookie 和原会话保持不变。临时链接、接续码和 Cookie 未记录到证据。
- Gateway 148 项、小程序原版 117 项、隔离 Mongo Web OAuth 14 组和小程序接续 7 组通过。PR CI 最初发现旧 GitHub 地址断言，修正到规范 Tuyan 地址后通过。Pages 首次遇到焦点测试偶发失败，针对测试及完整 CI 通过后重跑失败任务成功，没有修改代码绕过测试。

## 发布评审跟进：小程序 3.5.5

两项有效发现已经修复并加入行为回归：充值订单明确收到无不确定标志的 4xx 拒绝时可以重新尝试；网络、超时、5xx 或显式未知状态仍禁止重复下单。清理本地任务只清理当前账号记录，保留其他账号和旧版未归属记录。源码、编译 JS、发布元数据一致，小程序 119 项测试通过。

另外两条自动评审意见没有改动：账户页“返回原页面”按钮按用户要求保持删除；网关取消时 `clear()` 已包含 `{ clientHash: id }`，会删除同样带该字段的 `mini-client` 标记，前缀 `_id` 不会阻止清理。既有取消和回调竞争测试通过。

## 本地上传副本

`/Users/a1-6/WeChatProjects/miniprogram-9` 已同步到 3.5.5：215 个受控文件全部一致，更新 13 个，9 个本机私有/额外文件摘要不变。受控旧文件备份在仓库外，位置见 [同步清单](../evidence/miniprogram-3.5.5/local-sync.json)。上传副本 119 项测试通过；热更新后原 158 / 28 / 44 字输入及三个模型选择保持，账户页显示消费授权已连接。

## 交付边界

这次补齐了 GitHub 主线、生产后端部署和开发者工具到真实浏览器的 OAuth 接续验证。手机物理设备跨应用验证不等于开发者工具验证；微信上传、体验版、审核、正式发布仍是独立的平台状态。本次没有实际充值、付费模型调用、解绑或删除正式账号。原有用户截图只证明当时上传成功，不能用于声称新版已审核或发布。
