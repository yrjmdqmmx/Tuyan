# 本地验证记录 · 2026-09-21 · 模型目录 v21

工作目录：`/Users/a1-6/.config/superpowers/worktrees/paperbanana-tuyan/model-catalog-channels-20260921`，分支 `codex/model-catalog-channels-20260921`，基线 `ec24b730282c820881e206986df1f48b7dd98215`。桌面旧主 checkout 与微信上传副本未修改。

## 代码与数据

- 新增版本字段和逐渠道逐 ID 审核，95 个展示名称补齐具体型号 / 快照；所有 760 个既有 ID、默认值、角色、协议、能力、可选标记与基线 SHA-256 一致，未写入数据库或迁移任务。
- v21 仅修正名称、版本证据和渠道标签；沿用 v20 的新增 20 个静态型号、停用 6 个精确旧型号，以及 OpenRouter 新增 21 个同步型号的审核结论。本次没有新增 / 删除 / 更名 API ID。
- 四类优先级稳定排序，不在界面增加地域词；同类保留各入口原有相对次序。旧 `accessKind` 仍用于原有辅助标签。
- 1175 行对照表：760 个静态条目 + 415 个 OpenRouter 同步候选（含后续仍需能力过滤的图片条目）。126 行固定版本、139 行滚动别名、910 行固定性 / 对应版本待确认。后者多数已确认具体目录 SKU，但没有不可变快照承诺，不表示模型下架。
- 待确认表共 938 行，包含版本性质未确认及 28 个当前目标未确认的滚动别名。其中精确 ID 证据缺口 15 条（方舟旧型号 13 条，TokenDance 本次目录未出现 2 条）；不因缺口而删除身份或改写用户选择。

## 验证范围

- Web：457 项单测及渲染交互；后端：649 项；小程序：119 项；根级共享契约：20 项。共 1245 项，无真实付费推理。
- 新增回归涵盖 DeepSeek 直连 / 百炼 / OpenRouter 不同版本映射、OpenAI 默认快照与旧预览 ID、Mistral 别名、Claude 无日期固定快照、缺失 canonical_slug、图片 / 文本目录版本隔离、API ID 与显示名分离、全部静态身份和默认项不变；另覆盖跨渠道通用别名的错误发布日期清除（含旧缓存日期），避免 0423 显示 0813 日期。
- Web / Core 构建、Core 类型检查、小程序 TypeScript 编译、目录 / 参考图契约 / 对照表生成漂移检查通过。Web 仍有大 chunk 提示，未扩大发布范围；这不是推理或账号权限验证。
- 小程序目录按共享键列表压缩；完整字段与独立可变对象回归通过。包含 TS 的源目录约 1.51 MB，未压缩运行时代码约 1.10 MB，低于 1.5 MiB 保守预算。此处未进行微信开发者工具上传扫描或真机验收。
- 使用已安装 Chrome + Playwright 检查 1440×1050、390×844：所有外部请求被本地模拟拦截；DeepSeek 两个具体版本、滚动标记与 API ID 可见；浏览渠道不改当前选项；显式选择仍返回原 ID；退役型号不可选；OpenRouter 0423 与 0813 显示为独立条目且 0423 不再继承 0813 日期；无横向溢出及页面报错。截图与 qa.json 见本任务 model-version-audit 制品目录。

## 部署与权限

本轮付费推理调用 **0**。没有 push、PR 发布、Core / Laf / Web 部署、微信副本同步、上传、审核或发布。没有读取或更改 API 密钥，没有新增供应商账号授权，也没有以目录可见替代账号权益或真实调用证明。修改尚未上线；不据此断言当前线上运行状态。

来源日期、原始响应 SHA-256、逐项判断与公开阅读缺口保存在 `config/model-version-audit.json`；完整表和待确认表可从 `docs/model-version-audit-20260921.md` 打开。
