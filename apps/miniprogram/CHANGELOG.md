# 更新日志

## 3.5.1 (2026-09-14)

- 账户页复用已确认连接状态，进入页面时后台复核；合并并发状态请求，不再反复显示正在查询。
- 状态查询失败明确提示暂无法确认；账号切换、解除和重新授权清理旧状态与晚到响应。
- 模型设置中的观猹角色已连接时显示“管理授权”，未连接时显示“连接账户”，避免误导重复授权。

## 3.5.0 (2026-09-14)

- 修复微信原生按钮样式覆盖导致的渠道卡片变窄与图库分页竖排，长标题及按钮统一对齐。
- 三角色旁显示渠道凭据状态，缺失 Key 可直接定位；Key 按渠道共享，MiniMax 按区域区分。
- 模型选择改为渠道到通栏模型列表，保留搜索与厂商筛选，型号及详情按需展开。
- 首页原生轮播每 3 秒切换，触摸、输入、弹层及后台暂停，仅明确套用时更新正文。
- 教程渠道改为折叠标签，移除百炼默认路线；账户去除占位头像；清理 selectable 弃用属性。
- 网关 mini-status 404 提供独立说明，线上身份接续仍等待网关部署与真机验收。


## 图研Tuyan 3.4.0（2026-09-13）

- 优化生成页、生成设置、精修页和账户页的手机布局，减少常驻说明和嵌套卡片；模型限制默认折叠，真实上传校验与具体错误保留。
- 生成与精修共用可视化比例选择器，按当前模型和清晰度契约展示合法选项。设置使用紧凑固定操作区、独立滚动与键盘避让。
- 统一观猹首次默认为 Seedream 5.0 Pro / 1K，普通模式重复点击不再重置模型；保存的非敏感偏好可恢复，保留明确选择的 Lite 等模型。
- 修复精修比例/清晰度只更新索引的问题；页面、草稿、持久化和请求共用设置。修复冷启动恢复登录后消费授权未及时读取、重复 hideLoading 和 auth-panel 标签选择器警告。
- 账户按账号、身份绑定、消费授权、钱包与充值分层；解除连接需确认，账号切换后丢弃旧确认。保留原有身份登录、优化、上传、导出和任务恢复。
- 本版本为本地源码与原生模拟器验收；未上传、未审核、未正式发布，无真实支付或付费模型调用。

## 图研Tuyan 3.3.0（2026-09-10）

- 新增观猹身份登录与绑定入口，与 TokenDance 渠道授权分开。系统浏览器授权后，通过一次性接续码回到原小程序建立图研会话。
- 首次接入支持邮箱验证码创建账号；已有邮箱必须先登录原账号再明确绑定。解绑需邮箱验证码，唯一登录方式禁止解绑。
- 无密码账号可通过邮箱设置密码，或用邮箱验证码确认注销；保留二次确认、生命周期和账号隔离。
- 保留 3.2.0 上传 v2、四处优化、模型选择、钱包和原任务恢复。网关需支持小程序接续协议后才能启用；本轮未微信上传、未审核、未正式发布。

## 图研Tuyan 3.2.0（2026-09-10）

- 新增独立账户页：图研账号安全、观猹 TokenDance 渠道授权、余额与充值记录；一次性授权码仅驻留逻辑层，账户切换丢弃晚到响应。
- 同步 v18 模型目录（22 个渠道、739 条静态模型），新会话优先观猹与 Seedream 5.0 Pro；保留已有选择，参数由模型能力校验。
- 生成参考图与精修上传适配 v2：单张原图 20MiB、生成最多 8 张/合计 80MiB，SVG 5MiB；原图额度与模型处理限制分离，保留原图并提示不兼容原因。
- 四处输入优化接入 TokenDance 服务端授权，保留预览、采用、撤销；任务恢复沿用原 jobId、倒计时和已完成阶段，不自动重建计费任务。
- 保留 3.1.0 账号安全、精修结果、图库、导出与 owner 隔离。充值仅显式创建，待核对时禁止重复创建。
- 本版本仅源码、本地模拟器验收与受控副本同步；未上传、未审核、未正式发布。真实跨应用授权/支付/付费生成需另行验收。

## 图研Tuyan 3.1.0（2026-09-09）

- 保留 3.0.1 邮箱验证/重发、找回密码与改密；补齐邮箱验证只读观察、账号生命周期 v3 的处理中/需处理状态。
- 复用 v16 模型目录、21 个渠道、区域密钥、分型号与清晰度比例规则；补齐提交前角色、区域、输出格式和编辑专用型号校验。
- 新增方法、图注、负向提示词和精修指令的输入优化：先预览、明确采用、取消、手动重试和单步恢复原文。
- 新增原图精修：相册/聊天文件选择，PNG/JPG/WebP 与字节/尺寸校验，真实上传阶段、取消/重试、来源替换与清理。
- 精修设置独立消费精修能力；生成设置随当前草稿重算比例及所需密钥。精修结果支持详情、预览、保存和文件分享。
- 本机任务按不可变账号 ID 隔离；切换账号后旧任务、原图、优化请求不能回填。无法确认归属的历史缓存保留但不跨账号展示。
- 修复前后台轮询、精修来源刷新、图库筛选索引及新增业务错误反馈。TypeScript 与生成 JavaScript 一同交付。
- 本轮交付仅包括源码、本地验收与受控副本同步；未上传、未提审、未正式发布，未变更生产后端。供应商计费调用、邮箱实际投递及真机相册/分享需另行验证。

## 图研Tuyan 3.0.1（2026-08-23）

- 与 Web/iOS 同步标准账号安全：新增邮箱验证/重发、忘记密码与登录后修改密码，并提供稳定的冷却/错误反馈。
- 兼容现有任务、模型设置和账号删除；无需用户、数据库或本地存储迁移，既有会话保持有效。
- 本版本记录客户端实现与上传准备，不代表体验版、微信审核或正式发布。

## 图研Tuyan 1.0.0（2026-08-21）

- 微信端品牌升级为“图研Tuyan”，PaperBanana 技术标识、AppID、API 与本地数据键保持不变。
- 切换香港生产 API；Auth Gateway 精确支持微信来源。
- 新增 registry 驱动的五渠道模型目录、跨渠道路由、模型搜索、权益/生命周期/验证状态和失败关闭。
- 新增六套精选模板、十种比例、负向提示词、原子设置抽屉和较宽的分层工作台。
- 参考图库升级为 306 条服务端分页、搜索、双分面、diagram/plot 和跨页选择。
- 任务记录补齐来源端、路由、负向提示词、比例、阶段、错误和 objectKey。
- 新增独立“精修”一级入口，生成/精修能力字段独立消费。
- 新增账户隐私说明与永久删除流程；API Key 保持仅内存。
- 教程按线上 v9 设置指南重构为微信原生折叠章节。

## miniprogram-v0.2.0（2026-06-10）

微信小程序端大版本更新：功能全量对齐网站端 + 多页重构 + 健壮性整修。

### 架构重构
- 单页（index.ts 约 1950 行）拆为底部 tabBar 三页：**生成 / 记录 / 教程**，外加任务详情独立页
- 新增 4 个自定义组件：auth-panel（登录）、feedback-panel（反馈）、reference-library（参考图库）、stage-timeline（生成演化时间线）
- 纯逻辑下沉 `utils/`：config / api / session / constants / jobs / payload / media / reference-files / reference-mode
- node 单测体系（`tests/*.test.cjs`，6 个文件）：常量对齐、payload 语义、参考图模式、防御解析、签名 URL 稳定化

### 对齐网站端（清空 SYNC.md 小程序欠账）
- **输出清晰度 1K/2K/4K**：按 provider/图像模型过滤，2K/4K 出图后后端自动「精修放大」（已实测：2K 任务出现第 5 阶段"精修放大（2K）"）
- **百炼模型常量**同步官方真实模型（qwen3.7-max / wan2.7-image-pro / qwen3.7-plus 等）；参考图处理方式按 `mainModelCanReadImages` 固定派生，移除"自动选择"
- **检索设置**（不使用/自动/随机/手动）+ **手动参考图库**选卡（≤10）；上传参考图后检索自动锁定并提示
- **plot 任务**：数据统计图类别 → `taskName:'plot'`（独立渲染服务）
- **生成演化时间线**：规划/渲染/评审/精修放大各阶段与中间图、检索参考列表
- **使用教程页**（参数详解 + FAQ）+ **联系作者二维码**（长按识别）+ 关于

### UI / 交互
- 品牌墨绿主题（#315f51，与 web --sage 一致）：主按钮 / 模式开关 / tabBar 选中色 / 反馈悬浮球
- 反馈悬浮球可**按住拖动**（movable-view）；登录与反馈改为居中模态弹层
- 头部精简（后端异常才显示警示条）；快速上手案例双列紧凑
- 阶段中间图新增**保存图片**按钮；SVG 资产「下载文件」：下载到本地后弹微信文件分享（真机），模拟器自动退回复制链接
- SVG 模式下隐藏对其无效的「输出清晰度」「图像模型」选项

### 健壮性（多智能体审查确认后修复）
- 轮询竞态：提交先停旧轮询 + `loadJob` 校验 jobId，旧任务在途响应不再污染新任务/误停轮询
- tabBar 页隐藏时不轮询；登录态时序守卫（session epoch）；记录页跨账号在途响应丢弃
- `hydrateRecordJobs` 并发池（≤5，规避 wx.request 10 并发上限）
- 桶签名 URL 稳定化：消除轮询期间阶段图/结果图每 3s 闪烁
- data:URL 落盘去重 + 启动清理（USER_DATA_PATH 上限保护）；wxfile:// 路径直通
- 防御 JSON 解析（`coerceJsonResponse`）；相册权限被拒后 openSetting 引导
- `wx.getWindowInfo` 替代弃用的 getSystemInfoSync

### 端到端实测
- 登录 / 参考图上传（PUT 签名直传）/ createJob / 轮询 / stages / 出图全链路与部署后端契约一致
- 1K 任务 4 阶段直出；2K 任务 5 阶段含自动精修放大，百炼实际跑通未回退

### 域名白名单（上线前提）
- request：`https://yifbnnzrwmxn.sealoshzh.site` + `https://objectstorageapi.hzh.sealos.run`
- downloadFile：`https://objectstorageapi.hzh.sealos.run`（保存相册 / SVG 下载用）

## miniprogram-v0.1.0

初版：单页生成 + 任务记录、邮箱登录、四服务商 BYOK、参考图上传、本机最近任务。
